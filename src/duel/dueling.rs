//! Contains the state for a duel.

pub struct Dueling {
    white: Thinking,
    black: Thinking,
}

fn duel( &self) -> Dueling {
    make_oracles = (
        contender.clone(on_gpu=params.sim.use_gpu, test_mode=True),
        contender.clone(on_gpu=params.sim.use_gpu, test_mode=True),
    )

    let imagining = Imagine(make_oracles, record_trace)

    let white = Mind(gspec, oracles<1>, params.mcts);
    let black = Mind(gspec, oracles<2>, params.mcts);

    Duel {white, black}
}

